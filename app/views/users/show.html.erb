<div class="container">
 <div><%= image_tag  @image ,:size =>'200x200'%></div>
 <p>好きなトレーニング<%= @training %></p>
 <p><%= @nickname %>さんの投稿一覧</p>
    <% @tweets.each do |tweet| %>
     <%= render partial: "tweets/tweet", locals: { tweet: tweet } %>
    <% end %>
  <%= render partial: "tweets/recommend_user", locals: { users: @users } %>
          <%= paginate(@tweets) %>
    </div>

</div>
